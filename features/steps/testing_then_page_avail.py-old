from behave import *

from ScrapeHTML.test_manager import *
from ScrapeHTML.defined_values import *

'''
The following definitions relate to the Firefox browser.
'''

@then('we have a Firefox page which ran the canvas and font and screen response')
@then('we have a Firefox page which ran the canvas and clientRects and webgl and webRTC response')
@then('we have a Firefox page which ran the canvas and clientRects and screen and webgl response')
@then('we have a Firefox page which ran the canvas and clientRects and screen and webRTC response')
@then('we have a Firefox page which ran the canvas and clientRects and navigator and screen response')
@then('we have a Firefox page which ran the canvas and clientRects and navigator and webgl response')
@then('we have a Firefox page which ran the canvas and clientRects and navigator and webRTC response')
@then('we have a Firefox page which ran the canvas and clientRects and font and webgl and webRTC response')
@then('we have a Firefox page which ran the canvas and clientRects and font and screen and webgl response')
@then('we have a Firefox page which ran the canvas and clientRects and font and screen and webRTC response')
@then('we have a Firefox page which ran the canvas and clientRects and font and navigator response')
@then('we have a Firefox page which ran the canvas and clientRects and font and screen response')
@then('we have a Firefox page which ran the canvas and clientRects and font and webgl response')
@then('we have a Firefox page which ran the canvas and clientRects and font and webRTC response')
@then('we have a Firefox page which ran the canvas and clientRects and font and navigator and screen response')
@then('we have a Firefox page which ran the canvas and clientRects and font and navigator and webgl response')
@then('we have a Firefox page which ran the canvas and clientRects and font and navigator and webRTC response')
@then('we have a Firefox page which ran the battery response')
@then('we have a Firefox page which ran the battery and screen and webgl response')
@then('we have a Firefox page which ran the battery and screen and webRTC response')
@then('we have a Firefox page which ran the battery and webgl and webRTC response')
@then('we have a Firefox page which ran the battery and navigator and screen response')
@then('we have a Firefox page which ran the battery and navigator and webgl response')
@then('we have a Firefox page which ran the battery and navigator and webRTC response')
@then('we have a Firefox page which ran the battery and audio response')
@then('we have a Firefox page which ran the battery and canvas response')
@then('we have a Firefox page which ran the battery and clientRects response')
@then('we have a Firefox page which ran the battery and font response')
@then('we have a Firefox page which ran the battery and navigator response')
@then('we have a Firefox page which ran the battery and screen response')
@then('we have a Firefox page which ran the battery and webgl response')
@then('we have a Firefox page which ran the battery and webRTC response')
@then('we have a Firefox page which ran the battery and font and screen and webgl response')
@then('we have a Firefox page which ran the battery and font and screen and webRTC response')
@then('we have a Firefox page which ran the battery and font and navigator and webgl and webgl response')
@then('we have a Firefox page which ran the battery and font and navigator and webgl and webRTC response')
@then('we have a Firefox page which ran the battery and clientRects and screen and webgl response')
@then('we have a Firefox page which ran the battery and clientRects and screen and webRTC response')
@then('we have a Firefox page which ran the battery and clientRects and font response')
@then('we have a Firefox page which ran the battery and clientRects and navigator response')
@then('we have a Firefox page which ran the battery and clientRects and screen response')
@then('we have a Firefox page which ran the battery and clientRects and webgl response')
@then('we have a Firefox page which ran the battery and clientRects and webRTC response')
@then('we have a Firefox page which ran the battery and clientRects and webgl and webRTC response')
@then('we have a Firefox page which ran the battery and font and navigator and screen and webgl response')
@then('we have a Firefox page which ran the battery and font and navigator and screen and webRTC response')
@then('we have a Firefox page which ran the battery and font and navigator response')
@then('we have a Firefox page which ran the battery and font and screen response')
@then('we have a Firefox page which ran the battery and font and webgl response')
@then('we have a Firefox page which ran the battery and font and webRTC response')
@then('we have a Firefox page which ran the battery and font and webgl and webRTC response')
@then('we have a Firefox page which ran the battery and clientRects and navigator and screen response')
@then('we have a Firefox page which ran the battery and clientRects and navigator and webgl response')
@then('we have a Firefox page which ran the battery and clientRects and navigator and webRTC response')
@then('we have a Firefox page which ran the battery and clientRects and font and webgl and webgl response')
@then('we have a Firefox page which ran the battery and clientRects and font and webgl and webRTC response')
@then('we have a Firefox page which ran the battery and clientRects and font and screen and webgl response')
@then('we have a Firefox page which ran the battery and clientRects and font and screen and webRTC response')
@then('we have a Firefox page which ran the battery and clientRects and font and navigator response')
@then('we have a Firefox page which ran the battery and clientRects and font and screen response')
@then('we have a Firefox page which ran the battery and clientRects and font and webgl response')
@then('we have a Firefox page which ran the battery and clientRects and font and webRTC response')
@then('we have a Firefox page which ran the battery and clientRects and font and navigator and screen response')
@then('we have a Firefox page which ran the battery and clientRects and font and navigator and webgl response')
@then('we have a Firefox page which ran the battery and clientRects and font and navigator and webRTC response')
@then('we have a Firefox page which ran the battery and canvas and webgl and webRTC response')
@then('we have a Firefox page which ran the battery and canvas and screen and webgl response')
@then('we have a Firefox page which ran the battery and canvas and screen and webRTC response')
@then('we have a Firefox page which ran the battery and canvas and clientRects response')
@then('we have a Firefox page which ran the battery and canvas and font response')
@then('we have a Firefox page which ran the battery and canvas and navigator response')
@then('we have a Firefox page which ran the battery and canvas and screen response')
@then('we have a Firefox page which ran the battery and canvas and webgl response')
@then('we have a Firefox page which ran the battery and canvas and webRTC response')
@then('we have a Firefox page which ran the battery and canvas and navigator and screen response')
@then('we have a Firefox page which ran the battery and canvas and navigator and webgl response')
@then('we have a Firefox page which ran the battery and canvas and navigator and webRTC response')
@then('we have a Firefox page which ran the battery and canvas and font and navigator response')
@then('we have a Firefox page which ran the battery and canvas and font and screen response')
@then('we have a Firefox page which ran the battery and canvas and font and webgl response')
@then('we have a Firefox page which ran the battery and canvas and font and webRTC response')
@then('we have a Firefox page which ran the battery and canvas and clientRects and webgl and webgl response')
@then('we have a Firefox page which ran the battery and canvas and clientRects and webgl and webRTC response')
@then('we have a Firefox page which ran the battery and canvas and clientRects and screen and webgl response')
@then('we have a Firefox page which ran the battery and canvas and clientRects and screen and webRTC response')
@then('we have a Firefox page which ran the battery and canvas and clientRects and font response')
@then('we have a Firefox page which ran the battery and canvas and clientRects and navigator response')
@then('we have a Firefox page which ran the battery and canvas and clientRects and screen response')
@then('we have a Firefox page which ran the battery and canvas and clientRects and webgl response')
@then('we have a Firefox page which ran the battery and canvas and clientRects and webRTC response')
@then('we have a Firefox page which ran the battery and canvas and clientRects and navigator and screen response')
@then('we have a Firefox page which ran the battery and canvas and clientRects and navigator and webgl response')
@then('we have a Firefox page which ran the battery and canvas and clientRects and navigator and webRTC response')
@then('we have a Firefox page which ran the battery and canvas and clientRects and font and navigator response')
@then('we have a Firefox page which ran the battery and canvas and clientRects and font and screen response')
@then('we have a Firefox page which ran the battery and canvas and clientRects and font and webgl response')
@then('we have a Firefox page which ran the battery and canvas and clientRects and font and webRTC response')
@then('we have a Firefox page which ran the audio and webgl and webRTC response')
@then('we have a Firefox page which ran the audio response')
@then('we have a Firefox page which ran the battery and navigator and screen and webgl and webgl response')
@then('we have a Firefox page which ran the battery and navigator and screen and webgl and webRTC response')
@then('we have a Firefox page which ran the battery and font and navigator and screen response')
@then('we have a Firefox page which ran the battery and font and navigator and webgl response')
@then('we have a Firefox page which ran the battery and font and navigator and webRTC response')
@then('we have a Firefox page which ran the audio and screen and webgl and webRTC response')
@then('we have a Firefox page which ran the audio and screen and webgl response')
@then('we have a Firefox page which ran the audio and screen and webRTC response')
@then('we have a Firefox page which ran the audio and battery response')
@then('we have a Firefox page which ran the audio and canvas response')
@then('we have a Firefox page which ran the audio and clientRects response')
@then('we have a Firefox page which ran the audio and font response')
@then('we have a Firefox page which ran the audio and navigator response')
@then('we have a Firefox page which ran the audio and screen response')
@then('we have a Firefox page which ran the audio and webgl response')
@then('we have a Firefox page which ran the audio and webRTC response')
@then('we have a Firefox page which ran the audio and navigator and screen and webgl response')
@then('we have a Firefox page which ran the audio and navigator and screen and webRTC response')
@then('we have a Firefox page which ran the audio and navigator and screen response')
@then('we have a Firefox page which ran the audio and navigator and webgl response')
@then('we have a Firefox page which ran the audio and navigator and webRTC response')
@then('we have a Firefox page which ran the audio and font and webgl and webRTC response')
@then('we have a Firefox page which ran the audio and font and screen and webgl response')
@then('we have a Firefox page which ran the audio and font and screen and webRTC response')
@then('we have a Firefox page which ran the audio and font and navigator response')
@then('we have a Firefox page which ran the audio and font and screen response')
@then('we have a Firefox page which ran the audio and font and webgl response')
@then('we have a Firefox page which ran the audio and font and webRTC response')
@then('we have a Firefox page which ran the audio and font and navigator and screen response')
@then('we have a Firefox page which ran the audio and font and navigator and webgl response')
@then('we have a Firefox page which ran the audio and font and navigator and webRTC response')
@then('we have a Firefox page which ran the audio and clientRects and screen and webgl response')
@then('we have a Firefox page which ran the audio and clientRects and screen and webRTC response')
@then('we have a Firefox page which ran the audio and clientRects and font response')
@then('we have a Firefox page which ran the audio and clientRects and navigator response')
@then('we have a Firefox page which ran the audio and clientRects and screen response')
@then('we have a Firefox page which ran the audio and clientRects and webgl response')
@then('we have a Firefox page which ran the audio and clientRects and webRTC response')
@then('we have a Firefox page which ran the audio and clientRects and navigator and screen response')
@then('we have a Firefox page which ran the audio and clientRects and navigator and webgl response')
@then('we have a Firefox page which ran the audio and clientRects and navigator and webRTC response')
@then('we have a Firefox page which ran the audio and clientRects and font and navigator response')
@then('we have a Firefox page which ran the audio and clientRects and font and screen response')
@then('we have a Firefox page which ran the audio and clientRects and font and webgl response')
@then('we have a Firefox page which ran the audio and clientRects and font and webRTC response')
@then('we have a Firefox page which ran the audio and canvas and webgl and webgl response')
@then('we have a Firefox page which ran the audio and canvas and webgl and webRTC response')
@then('we have a Firefox page which ran the audio and canvas and clientRects response')
@then('we have a Firefox page which ran the audio and canvas and font response')
@then('we have a Firefox page which ran the audio and canvas and navigator response')
@then('we have a Firefox page which ran the audio and canvas and screen response')
@then('we have a Firefox page which ran the audio and canvas and webgl response')
@then('we have a Firefox page which ran the audio and canvas and webRTC response')
@then('we have a Firefox page which ran the audio and canvas and navigator and navigator response')
@then('we have a Firefox page which ran the audio and canvas and navigator and screen response')
@then('we have a Firefox page which ran the audio and canvas and navigator and webgl response')
@then('we have a Firefox page which ran the audio and canvas and navigator and webRTC response')
@then('we have a Firefox page which ran the audio and canvas and clientRects and clientRects response')
@then('we have a Firefox page which ran the audio and canvas and clientRects and font response')
@then('we have a Firefox page which ran the audio and canvas and clientRects and navigator response')
@then('we have a Firefox page which ran the audio and canvas and clientRects and screen response')
@then('we have a Firefox page which ran the audio and canvas and clientRects and webgl response')
@then('we have a Firefox page which ran the audio and canvas and clientRects and webRTC response')
@then('we have a Firefox page which ran the audio and battery and webgl and webRTC response')
@then('we have a Firefox page which ran the audio and battery and screen and webgl response')
@then('we have a Firefox page which ran the audio and battery and screen and webRTC response')
@then('we have a Firefox page which ran the audio and battery and canvas response')
@then('we have a Firefox page which ran the audio and battery and clientRects response')
@then('we have a Firefox page which ran the audio and battery and font response')
@then('we have a Firefox page which ran the audio and battery and navigator response')
@then('we have a Firefox page which ran the audio and battery and screen response')
@then('we have a Firefox page which ran the audio and battery and webgl response')
@then('we have a Firefox page which ran the audio and battery and webRTC response')
@then('we have a Firefox page which ran the audio and battery and navigator and screen response')
@then('we have a Firefox page which ran the audio and battery and navigator and webgl response')
@then('we have a Firefox page which ran the audio and battery and navigator and webRTC response')
@then('we have a Firefox page which ran the audio and battery and clientRects and clientRects response')
@then('we have a Firefox page which ran the audio and battery and clientRects and font response')
@then('we have a Firefox page which ran the audio and battery and clientRects and navigator response')
@then('we have a Firefox page which ran the audio and battery and clientRects and screen response')
@then('we have a Firefox page which ran the audio and battery and clientRects and webgl response')
@then('we have a Firefox page which ran the audio and battery and clientRects and webRTC response')
@then('we have a Firefox page which ran the audio and battery and canvas and webgl and webRTC response')
@then('we have a Firefox page which ran the audio and battery and canvas and screen and webgl response')
@then('we have a Firefox page which ran the audio and battery and canvas and screen and webRTC response')
@then('we have a Firefox page which ran the audio and battery and canvas and canvas response')
@then('we have a Firefox page which ran the audio and battery and canvas and clientRects response')
@then('we have a Firefox page which ran the audio and battery and canvas and font response')
@then('we have a Firefox page which ran the audio and battery and canvas and navigator response')
@then('we have a Firefox page which ran the audio and battery and canvas and screen response')
@then('we have a Firefox page which ran the audio and battery and canvas and webgl response')
@then('we have a Firefox page which ran the audio and battery and canvas and webRTC response')
@then('we have a Firefox page which ran the audio and battery and canvas and navigator and screen response')
@then('we have a Firefox page which ran the audio and battery and canvas and navigator and webgl response')
@then('we have a Firefox page which ran the audio and battery and canvas and navigator and webRTC response')
@then('we have a Firefox page which ran the audio and battery and canvas and font and navigator response')
@then('we have a Firefox page which ran the audio and battery and canvas and font and screen response')
@then('we have a Firefox page which ran the audio and battery and canvas and font and webgl response')
@then('we have a Firefox page which ran the audio and battery and canvas and font and webRTC response')
@then('we have a Firefox page which ran the audio and battery and canvas and clientRects and font response')
@then('we have a Firefox page which ran the audio and battery and canvas and clientRects and navigator response')
@then('we have a Firefox page which ran the audio and battery and canvas and clientRects and screen response')
@then('we have a Firefox page which ran the audio and battery and canvas and clientRects and webgl response')
@then('we have a Firefox page which ran the audio and battery and canvas and clientRects and webRTC response')
@then('we have a Firefox page which ran the clientRects and font and navigator and screen response')
@then('we have a Firefox page which ran the clientRects and font and navigator and webgl response')
@then('we have a Firefox page which ran the clientRects and font and navigator and webRTC response')
@then('we have a Firefox page which ran the clientRects and font and navigator and screen and webgl response')
@then('we have a Firefox page which ran the clientRects and font and navigator and screen and webRTC response')
@then('we have a Firefox page which ran the clientRects and font and navigator and webgl and webRTC response')
@then('we have a Firefox page which ran the clientRects and font and screen and webgl response')
@then('we have a Firefox page which ran the clientRects and font and screen and webRTC response')
@then('we have a Firefox page which ran the clientRects and font and webgl and webRTC response')
@then('we have a Firefox page which ran the clientRects and battery response')
@then('we have a Firefox page which ran the clientRects and audio response')
@then('we have a Firefox page which ran the clientRects and canvas response')
@then('we have a Firefox page which ran the clientRects and font response')
@then('we have a Firefox page which ran the clientRects and navigator response')
@then('we have a Firefox page which ran the clientRects and screen response')
@then('we have a Firefox page which ran the clientRects and webgl response')
@then('we have a Firefox page which ran the clientRects and webRTC response')
@then('we have a Firefox page which ran the clientRects response')
@then('we have a Firefox page which ran the font and navigator and screen and webgl response')
@then('we have a Firefox page which ran the font and navigator and screen and webRTC response')
@then('we have a Firefox page which ran the font and navigator and screen and webgl and webRTC response')
@then('we have a Firefox page which ran the font and navigator and webgl and webRTC response')
@then('we have a Firefox page which ran the font and battery response')
@then('we have a Firefox page which ran the font and audio response')
@then('we have a Firefox page which ran the font and canvas response')
@then('we have a Firefox page which ran the font and clientRect response')
@then('we have a Firefox page which ran the font and navigator response')
@then('we have a Firefox page which ran the font and screen response')
@then('we have a Firefox page which ran the font and webgl response')
@then('we have a Firefox page which ran the font and webRTC response')
@then('we have a Firefox page which ran the font response')
@then('we have a Firefox page which ran the navigator and battery response')
@then('we have a Firefox page which ran the navigator and audio response')
@then('we have a Firefox page which ran the navigator and canvas response')
@then('we have a Firefox page which ran the navigator and clientRect response')
@then('we have a Firefox page which ran the navigator and font response')
@then('we have a Firefox page which ran the navigator and screen response')
@then('we have a Firefox page which ran the navigator and webgl response')
@then('we have a Firefox page which ran the navigator and webRTC response')
@then('we have a Firefox page which ran the navigator and screen and webgl and webRTC response')
@then('we have a Firefox page which ran the navigator response')
@then('we have a Firefox page which ran the screen and battery response')
@then('we have a Firefox page which ran the screen and audio response')
@then('we have a Firefox page which ran the screen and canvas response')
@then('we have a Firefox page which ran the screen and clientRect response')
@then('we have a Firefox page which ran the screen and font response')
@then('we have a Firefox page which ran the screen and navigator response')
@then('we have a Firefox page which ran the screen and webgl response')
@then('we have a Firefox page which ran the screen and webRTC response')
@then('we have a Firefox page which ran the no availHeight or availWidth or colorDepth values interference response')
@then('we have a Firefox page which ran the availHeight response')
@then('we have a Firefox page which ran the availWidth response')
@then('we have a Firefox page which ran the colorDepth response')
@then('we have a Firefox page which ran the devicePixelRatio response')
@then('we have a Firefox page which ran the availHeight and availWidth values response')
@then('we have a Firefox page which ran the availHeight and colorDepth values response')
@then('we have a Firefox page which ran the availHeight and devicePixelRatio values response')
@then('we have a Firefox page which ran the availWidth and colorDepth values response')
@then('we have a Firefox page which ran the availWidth and devicePixelRatio values response')
@then('we have a Firefox page which ran the colorDepth and devicePixelRatio values response')
@then('we have a Firefox page which ran the availHeight and availWidth and colorDepth values response')
@then('we have a Firefox page which ran the availHeight and availWidth and devicePixelRatio values response')
@then('we have a Firefox page which ran the availHeight and devicePixelRatio and colorDepth values response')
@then('we have a Firefox page which ran the availWidth and devicePixelRatio and colorDepth values response')
@then('we have a Firefox page which ran the availHeight and availWidth and devicePixelRatio and colorDepth values response')
@then('we have a Firefox page which ran the screen response')
@then('we have a Firefox page which ran the webgl and battery response')
@then('we have a Firefox page which ran the webgl and audio response')
@then('we have a Firefox page which ran the webgl and canvas response')
@then('we have a Firefox page which ran the webgl and clientRect response')
@then('we have a Firefox page which ran the webgl and font response')
@then('we have a Firefox page which ran the webgl and navigator response')
@then('we have a Firefox page which ran the webgl and screen response')
@then('we have a Firefox page which ran the webgl and webRTC response')
@then('we have a Firefox page which ran the no parameter and buffer response')
@then('we have a Firefox page which ran the parameter response')
@then('we have a Firefox page which ran the buffer response')
@then('we have a Firefox page which ran the parameter and buffer values response')
@then('we have a Firefox page which ran the webgl response')
@then('we have a Firefox page which ran the webrtc and battery response')
@then('we have a Firefox page which ran the webrtc and audio response')
@then('we have a Firefox page which ran the webrtc and canvas response')
@then('we have a Firefox page which ran the webrtc and clientRect response')
@then('we have a Firefox page which ran the webrtc and font response')
@then('we have a Firefox page which ran the webrtc and navigator response')
@then('we have a Firefox page which ran the webrtc and screen response')
@then('we have a Firefox page which ran the webrtc and webgl response')
@then('we have a Firefox page which ran the webRTC response')
@then('we have a Firefox page which ran the canvas and font response')
@then('we have a Firefox page which ran the canvas and battery response')
@then('we have a Firefox page which ran the canvas and audio response')
@then('we have a Firefox page which ran the canvas and clientRects response')
# @then('we have a Firefox page which ran the canvas and font response')
@then('we have a Firefox page which ran the canvas and navigator response')
@then('we have a Firefox page which ran the canvas and webgl response')
@then('we have a Firefox page which ran the canvas and webRTC response')
@then('we have a Firefox page which ran the canvas response')
@then('we have a Firefox page which ran the canvas and screen response')
@then('we have a Firefox page which ran the no offsetHeight and offsetWidth response')
@then('we have a Firefox page which ran the offsetHeight response')
@then('we have a Firefox page which ran the offsetWidth response')
@then('we have a Firefox page which ran the offsetHeight and offsetWidth values response')
@then('we have a Firefox page which ran the no toBlob and toDataURL response')
@then('we have a Firefox page which ran the toBlob response')
@then('we have a Firefox page which ran the toDataURL response')
@then('we have a Firefox page which ran the toBlob and toDataURL values response')
def we_have_a_firefox_page_which_ran_the_toBlob_and_toDataURL_values_response(context):
    assert test_manager.html_puller_firefox.html_source != "<html></html>"