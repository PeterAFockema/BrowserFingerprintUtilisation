Feature: Checking HTMLElement offset variations for effects on fingerprinting responses
    Scenario: Preliminary Firefox test results for no extension to change HTMLElement offset values
    #    When we view the Firefox page with no offsetHeight or offsetWidth values interference
    #    Then we have a Firefox page which ran the offsetHeight and offsetWidth response
    #    Then the Firefox offsetHeight and offsetWidth values have been recorded
    #    Then we will log the no offsetHeight and offsetWidth testing time in the saved file
    #    Then the Firefox font value is saved
    #    Then the visitor id for no offsetHeight and offsetWidth value is saved
    
    Scenario: Preliminary Firefox test results for an extension to change the Prototype offsetHeight value
    #    When we view the Firefox page with some offsetHeight values interference
    #    Then we have a Firefox page which ran the offsetHeight response
    #    Then the Firefox font value has been recorded
    #    Then we will log the offsetHeight testing time in the saved file
    #    Then the Firefox font value is saved
    #    Then the visitor id for offsetHeight value variation is saved
    
    Scenario: Preliminary Firefox test results for an extension to change the Prototype offsetWidth value
    #    When we view the Firefox page with some offsetWidth values interference
    #    Then we have a Firefox page which ran the offsetWidth response
    #    Then the Firefox font value has been recorded
    #    Then we will log the offsetWidth testing time in the saved file
    #    Then the Firefox font value is saved
    #    Then the visitor id for offsetWidth value variation is saved

    Scenario: Preliminary Firefox test results for an extension to change both Prototype offsetHeight and offsetWidth value
    #    When we view the Firefox page with offsetHeight and offsetWidth values interference
    #    Then we have a Firefox page which ran the offsetHeight and offsetWidth values response
    #    Then the Firefox font value has been recorded
    #    Then we will log the offsetHeight and offsetWidth values testing time in the saved file
    #    Then the Firefox font value is saved
    #    Then the visitor id for offsetHeight and offsetWidth values variation value is saved